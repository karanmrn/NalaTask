[project]
name = "nala-analytics"
version = "0.1.0"
description = "NALA analytics engineering assessment: dbt transformation layer for payments, fincrime and product analytics"
requires-python = ">=3.11,<3.13"
dependencies = [
    "dbt-core>=1.12,<1.13",
    "dbt-duckdb>=1.10",
    "dbt-snowflake>=1.10",
    "dbt-metricflow>=0.14",
    "sqlfluff>=3.3",
    "sqlfluff-templater-dbt>=3.3",
    "pre-commit>=4",
]

[tool.uv]
package = false
